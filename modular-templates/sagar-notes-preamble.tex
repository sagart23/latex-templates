% sagar-notes-preamble.tex
% Shared preamble for article.tex and notex.tex

% ============================================================
% PACKAGES (already loaded before this file)
% ============================================================

\usepackage{enumitem}
\setlist[enumerate]{label=(\alph*)}
\setlist[itemize]{label=\textbullet}

\usepackage{multicol}

% ============================================================
% GRAPHICS
% ============================================================

\graphicspath{{./Images/}{./figures/}}

% ============================================================
% HEADERS & FOOTERS
% ============================================================

\usepackage{fancyhdr}
\usepackage{lastpage}

\pagestyle{fancy}
\fancyhf{}

\fancyhead[LE]{\sffamily\bfseries\color{primaryblue}\leftmark}
\fancyhead[RO]{\sffamily\bfseries\color{primaryblue}\rightmark}
\fancyhead[LO,RE]{\sffamily\small The Cathedral \& John Connon School}

\fancyfoot[LE,RO]{\sffamily\bfseries Page \thepage\ of \pageref{LastPage}}
\fancyfoot[LO,RE]{\sffamily\small Physics \& Mathematics Department}

\renewcommand{\headrulewidth}{0.5pt}
\renewcommand{\headrule}{%
  \hbox to\headwidth{%
    \color{primaryblue}\leaders\hrule height \headrulewidth\hfill}}

% FIX: ensure section marks work properly
\renewcommand{\sectionmark}[1]{\markboth{#1}{}}

% ============================================================
% SECTION STYLING
% ============================================================

\usepackage{sectsty}
\sectionfont{\sffamily\Large\bfseries\color{primaryblue}}
\subsectionfont{\sffamily\large\bfseries\color{secondarygreen!80!black}}
\subsubsectionfont{\sffamily\normalsize\bfseries\color{gray!70!black}}

% ============================================================
% CONDITIONAL FLAGS
% ============================================================

\newif\ifteacher
\newif\ifhint
\newif\ifanswer

% Default values (can override in main file)
\teacherfalse
\hintfalse
\answerfalse

% Helper commands
\newcommand{\teacheronly}[1]{\ifteacher #1\fi}
\newcommand{\hintonly}[1]{\ifhint #1\fi}
\newcommand{\answeronly}[1]{\ifanswer #1\fi}

% ============================================================
% PDF bookmarks improvement
% ============================================================

\usepackage{bookmark}
